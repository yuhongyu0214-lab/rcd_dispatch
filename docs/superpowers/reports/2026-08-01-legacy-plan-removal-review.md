# 旧版方案清场与引用治理报告

- 日期：2026-08-01（Asia/Shanghai）
- 清场分支：`feature/v2-legacy-doc-cleanup`
- 删除前基线：`feature/v2-gate3r-infrastructure-remediation @ 66fac13`
- 用户授权：完成融合或与 V2 冲突的旧方案从工作树删除；以后不得引用旧方案指导 V2
- 恢复方式：仅通过 Git 恢复用于审计，不恢复为现行权威

## 1. 审查结论

旧版标签不完整。归档目录中的两份旧执行文件已有 `SUPERSEDED` 标签，但工作树根部仍存在
未醒目标注的旧生产升级、高德、位置和 demo 契约文件；旧 Tair 文件虽有 V2 覆盖声明，
正文仍保留旧变量、旧 TTL 和旧运行平台入口。这种“正文可执行、顶部说别执行”的结构
仍会误导人工或 Agent，因此不能继续保留。

本轮采用以下规则：

1. 已完整融合到 V2 权威的旧文档，删除旧全文；
2. 与 V2 冲突的旧路线，删除旧全文；
3. 尚未完整融合、但仍用于维护现有 V1 Adapter 的来源证据，保留并加醒目的非权威标签；
4. 历史决策继续保存在 Git 和 `infrastructure-decision-log.md`，不通过保留旧施工图来证明历史。

## 2. 已删除文件与替代关系

| 旧文件类型 | 删除原因 | 当前唯一替代 |
|---|---|---|
| 两份 2026-07-04 旧升级执行文件及其归档说明 | 会驱动旧阶段、旧 Schema、小程序、WebSocket 和复杂部署路线 | V2 PRD、数据架构、并行开发设计与 Gate 3-R 技术实施计划 |
| `production-upgrade-plan.md` | 仍定义小程序、Top N、接单和旧生产闭环 | PRD V2、基础设施 V2、部署指南 V2、运维指南 V2 |
| `production-location-protocol.md` | 以微信小程序和后台定位为主，与前台 H5、锁屏不持续定位裁决冲突 | 数据架构 V2 §7.1、API 契约 V2 位置接口 |
| `production-amap-strategy.md` | 含旧候选、旧降级数字和旧部署平台变量入口 | PRD V2 ETA 规则、数据架构 V2、环境变量权限矩阵 |
| `production-tair-key-design.md` | 含旧 TTL、旧变量、无环境前缀示例和旧部署入口 | 数据架构 V2、基础设施 V2、受控 Tair 连接验收计划、现行代码测试 |
| `2026-07-06-e2e-closure-plan.md` | 属于 V1 闭环计划，包含接单、Top N 和旧定位频率 | PRD V2 并行开发设计及当前 Gate 计划 |
| demo API/UI 两份历史稿 | 可复用的响应、视觉 token 和布局已完整写入 V2 权威，剩余内容属于旧业务 | API 契约 V2、项目规则 V2 |

## 3. 保留但降权的材料

| 文件 | 保留原因 | 使用边界 |
|---|---|---|
| `production-field-mapping.md` | 现有 V1 Adapter 仍需追溯外部字段映射 | 只维护 V1 Adapter；不能定义 V2 CanonicalOrder、API 或状态机 |
| `人车单-RDS-浏览器插件-字段对接优化方案.md` | 浏览器插件兼容链路仍需字段来源说明 | 只作来源材料；V2 以数据架构和 API 契约为准 |
| `执行结果1_字段可用性评估.xlsx` | 原始字段评估证据 | 不能单独成为任何领域权威 |

两份 Markdown 已在文件顶部加红线声明；二进制评估表由 V1 索引明确降权。

## 4. 被撤销平台引用规则

旧境外 Demo 平台已撤销为 V2 运行基线。以后：

- 只允许在 `infrastructure-v2.md` 和 `infrastructure-decision-log.md` 中以“已撤销、非生产基线”的方式出现；
- 禁止在变量配置、部署指南、验收计划、运行手册或新实施报告中把它写成入口；
- “app 服务”必须写明为“阿里云 ECS 上的 Next.js app 容器”，不得使用平台含糊词；
- 若有效文档或代码再次引用已删除旧路径，立即停止实施并按阻断级文档缺陷处理。

## 5. 恢复与风险

删除只影响 Git 工作树中的旧文档全文，不删除 Git 历史、现有代码、数据库、云资源或外部部署。
需要审计旧结论时，从删除前基线 `66fac13` 读取，不得把文件恢复到当前分支继续施工。

代码注释中的旧路径已在应用代码所有者分支 `feature/v2-legacy-reference-cleanup @ 42d6064`
独立清理。该提交只改引用、不改逻辑，必须在本文档清场提交之后合入，不能把代码文件混入
本基建文档提交。
