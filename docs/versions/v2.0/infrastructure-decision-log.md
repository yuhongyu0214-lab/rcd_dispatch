# 人车单基础设施决策日志

> 文档版本：`RCD-INFRA-DECISIONS-V2.0-R3-20260808`
>
> 状态：Gate 3-R R7 已冻结；不表示云资源已经创建或生产验收已经通过
>
> 权威范围：基础设施平台决策、替代关系、影响和退出条件

## 1. 使用规则

- 决策状态只能是 `PROPOSED / ACCEPTED / SUPERSEDED / REJECTED`。
- 被替代的决策保留原记录，通过 `SUPERSEDED` 和关联新决策说明现状，不通过删除制造“从未发生”。
- 本日志不重新定义产品、Schema、API、状态机或兼容策略。
- “ACCEPTED”表示方向已经批准，不表示相应云资源已创建或生产验收已通过。
- 资源实时状态未独立核验时必须明确写“未核验”。

## 2. 决策记录

### INFRA-001：阿里云作为中国大陆正式生产主线

- 决策编号：`INFRA-001`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：Railway 的网络、套餐和中国大陆生产边界不适合作为本项目长期正式基线。
- 决定：正式生产架构以阿里云为唯一主线；具体资源必须重新盘点、建设和验收。
- 不选择什么：Railway 与阿里云双生产主线；把历史 Demo 直接升级为生产。
- 影响：R4～R7 重新冻结基建档案；Gate 3 在阿里云完成重新验收前保持暂停。
- 回滚或退出条件：若阿里云不再满足合规、网络或成本要求，必须新增决策替代，不得静默切换。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-002：Railway 正式生产裁决被替代

- 决策编号：`INFRA-002`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：Gate 3 曾将 Railway 升格为正式 app/worker 平台，但部署来自未提交工作区并存在套餐和网络边界问题。
- 决定：撤销 Railway 正式生产结论；已有部署、测试和部署 ID 只作为历史 Demo 与审计证据。
- 不选择什么：删除历史证据；继续把 Railway runbook 当正式生产手册。
- 影响：Railway 专属配置不得进入阿里云生产基线；平台无关 worker/outbox 成果仍可独立终审。
- 回滚或退出条件：无自动回滚；若未来重新评估 Railway，必须新建决策并重新验收。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-003：CloudBase 仅用于隔离预览和验证

- 决策编号：`INFRA-003`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：CloudBase 可能便于临时预览，但不能形成第二套生产事实或秘密边界。
- 决定：如保留 CloudBase，只允许假数据或脱敏后的独立测试数据，不得拥有生产秘密和生产数据权限。
- 不选择什么：CloudBase 作为生产数据库、生产调度环境或正式数据副本。
- 影响：是否继续作为持续环境由 `INFRA-017` 裁决；如未来临时启用，仍必须独立账号、数据和凭据。
- 回滚或退出条件：发现生产数据、秘密或生产网络权限时立即停止并清理。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-004：RDS PostgreSQL 是唯一业务事实库

- 决策编号：`INFRA-004`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：订单、调度、执行、预警和审计必须有唯一、可事务提交的事实来源。
- 决定：正式业务事实只保存于 RDS PostgreSQL。
- 不选择什么：Redis/Tair、CloudBase、Railway 数据库或日志平台成为第二事实库。
- 影响：所有缓存和降级必须服从数据库事实；数据库恢复是最高优先级能力。
- 回滚或退出条件：更换数据库产品属于新的重大架构决策和数据迁移项目。
- 关联文档：[数据架构 V2](data-architecture-v2.md)、[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-005：Redis/Tair 是 V2 第一版生产必需组件

- 决策编号：`INFRA-005`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：实时位置、在线状态、ETA 短缓存和短锁是 V2 滚动调度所需能力。
- 决定：第一版生产部署 Redis/Tair，并保持“短期实时数据，不保存业务事实”的边界。
- 不选择什么：完全移除 Redis 后仍声称 V2 实时调度完整；把 Redis 当持久业务库。
- 影响：必须建设连接、监控、故障降级和恢复后缓存重建。
- 回滚或退出条件：只有业务与数据权威另行证明替代方案后才能更换。
- 关联文档：[数据架构 V2](data-architecture-v2.md)、[运维指南 V2](operations-guide-v2.md)
- 批准人：用户

### INFRA-006：当前应用采用 ECS + Docker + Nginx

- 决策编号：`INFRA-006`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：当前 Next.js 模块化单体不需要微服务或 Kubernetes。
- 决定：ECS 承载 Docker；Nginx 提供 HTTPS 与入口转发；app/worker/migration 使用同一镜像的不同角色。
- 不选择什么：Kubernetes、第二套后端、为基础设施迁移拆微服务。
- 影响：第一版拓扑简单，但单 ECS 模式存在明确单点。
- 回滚或退出条件：容量、可用性或组织复杂度经证据证明当前模式不足后新建决策。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-007：ACR 是正式生产镜像来源

- 决策编号：`INFRA-007`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：生产部署必须把代码提交、镜像和回退版本关联起来。
- 决定：生产镜像进入 ACR，以 commit SHA 和 digest 形成不可变追溯链。
- 不选择什么：从开发机复制代码；从未提交工作区直接部署；仅用可变 `latest` 标识生产版本。
- 影响：构建、发布和回退必须记录镜像 digest。
- 回滚或退出条件：更换镜像仓库需保证等价的不可变、权限和审计能力。
- 关联文档：[部署指南 V2](deployment-guide-v2.md)
- 批准人：用户

### INFRA-008：集中日志是生产硬要求

- 决策编号：`INFRA-008`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：单机文件或临时终端日志无法支撑生产查询、告警和事故追溯。
- 决定：生产必须集中采集结构化日志；第一版产品选择由 `INFRA-018` 冻结为阿里云 SLS，但 SLS 不是业务契约。
- 不选择什么：只保留容器本地日志；把业务操作审计和运行日志混成一种记录。
- 影响：需确定保留期、脱敏、查询、告警和费用预算。
- 回滚或退出条件：若更换日志产品，必须保持结构化、集中保存、查询、告警和权限能力。
- 关联文档：[运维指南 V2](operations-guide-v2.md)
- 批准人：用户

### INFRA-009：app、worker、migration 三项职责分离

- 决策编号：`INFRA-009`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：多个常驻进程自行迁移会产生并发、权限和回退风险。
- 决定：app/worker 常驻，migration 每次发布最多运行一次；三者使用同一镜像但启动命令、权限和生命周期不同。
- 不选择什么：app/worker 启动时自动并发迁移；三个角色使用不同代码版本。
- 影响：发布必须在 migration 成功后再启动新 app/worker。
- 回滚或退出条件：任何编排方式变化都必须保持一次迁移和职责隔离。
- 关联文档：[部署指南 V2](deployment-guide-v2.md)
- 批准人：用户

### INFRA-010：Gate 3-R 在原 Gate 3 内执行

- 决策编号：`INFRA-010`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：Gate 0～Gate 2 和第一轮并行已有可保留成果，问题集中于 Gate 3 基建方向和未终审现场。
- 决定：不从 Gate 0 重来；保护平台无关候选成果，在 Gate 3 内完成基础设施重定向和重新验收。
- 不选择什么：抹除历史 Gate；把所有成果一起推倒；把未终审候选直接宣布通过。
- 影响：Gate 3 保持暂停，R0～R7 串行执行。
- 回滚或退出条件：若后续发现上游权威存在阻断级冲突，回到对应 Gate 裁决，而不是静默修改文档。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-011：第二轮并行继续冻结

- 决策编号：`INFRA-011`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：Gate 3 尚未在阿里云基线下重新通过。
- 决定：Web、司机端和观测等第二轮并行分支在 Gate 3-R 与 Gate 3 重新通过前不得创建或放行。
- 不选择什么：以 Railway Demo 或文档初稿作为第二轮并行的放行依据。
- 影响：当前只允许获准的文档和基建纠偏活动。
- 回滚或退出条件：Gate 3-R 文档冻结、阿里云实施验收及 Gate 3 终审通过。
- 关联文档：[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-012：当前 V2 不引入多租户等新产品范围

- 决策编号：`INFRA-012`
- 日期：2026-07-29
- 状态：`ACCEPTED`
- 背景：基础设施迁移不能借机扩大产品范围。
- 决定：多租户、计费、复杂多组织权限、微服务和分布式数据库不进入当前 V2。
- 不选择什么：以“为未来准备”为理由提前建设上述能力。
- 影响：基础设施保持满足现有 PRD 的最小复杂度。
- 回滚或退出条件：产品权威明确批准新范围后，另行启动需求、数据、API 和基建设计。
- 关联文档：[PRD V2](prd-v2.md)、[基础设施架构 V2](infrastructure-v2.md)
- 批准人：用户

### INFRA-013：生产地域跟随正式 RDS

- 决策编号：`INFRA-013`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：ECS、RDS、Tair 原则上使用正式 RDS 的同一地域和同一 VPC；具体地域名称只在只读核验后登记，不从实例编号或历史记录猜测。
- 影响：未核验正式 RDS 地域前不得采购或部署跨地域依赖。
- 批准人：用户

### INFRA-014：第一版采用单 ECS 试运行

- 决策编号：`INFRA-014`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：第一版使用单 ECS 承载 Nginx、app 和单副本 worker，接受主机单点与维护窗口中断。
- 边界：RDS、Tair、ACR 和 SLS 不得只存在于该 ECS；不得宣传为高可用或无损滚动。
- 升级条件：容量、故障或可用性证据证明需要时，再立项多实例与负载均衡。
- 批准人：用户

### INFRA-015：第一版恢复目标

- 决策编号：`INFRA-015`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：设计目标为 `RPO ≤ 5 分钟`、`RTO ≤ 2 小时`。
- 边界：备份、时间点恢复和完整恢复演练通过前，只能称为目标，不得对外承诺已经达到。
- 批准人：用户

### INFRA-016：数据与日志保留期

- 决策编号：`INFRA-016`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：司机历史位置 90 天、业务操作审计 1 年、运行日志 30 天、安全日志 180 天。
- 边界：自动删除必须受控、可审计并满足适用合规；法律、事故保全或业务争议要求更长保存时，按获准冻结/保全流程执行。
- 批准人：用户

### INFRA-017：CloudBase 不作为持续环境

- 决策编号：`INFRA-017`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：当前停止把 CloudBase 作为持续开发、预览或生产环境；未来确有临时预览需要时，以假数据或脱敏独立数据重新批准启用。
- 边界：本决策不授权删除现有 CloudBase 资源；历史证据继续保留，任何资源清理需单独授权。
- 批准人：用户

### INFRA-018：第一版集中日志使用 SLS

- 决策编号：`INFRA-018`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：第一版使用阿里云 SLS 集中采集 Nginx、app、worker、Docker 和系统结构化日志。
- 边界：启用预算、用量和异常支出告警；业务操作审计仍以 PostgreSQL 为事实来源。
- 批准人：用户

### INFRA-019：第一版不启用 OSS

- 决策编号：`INFRA-019`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：当前 PRD 没有文件业务，第一版不启用 OSS。
- 退出条件：未来产品权威批准文件业务后，重新进行数据、权限、生命周期和费用设计。
- 批准人：用户

### INFRA-020：实施型事项延后确定

- 决策编号：`INFRA-020`
- 日期：2026-07-30
- 状态：`ACCEPTED`
- 决定：维护窗口、正式域名与证书、秘密托管、演练频率、通知对象和责任人在 Gate 3-R 资源盘点与实施步骤中确定。
- 边界：这些事项未确定前不得部署正式生产，也不得把占位值、默认账号或个人凭据写成生产配置。
- 批准人：用户

### INFRA-021：现有 RDS/Tair 仅复用为 V2 预生产

- 决策编号：`INFRA-021`
- 日期：2026-07-31
- 状态：`ACCEPTED`
- 决定：现有上海 RDS/Tair 中的历史生产数据没有继续生产价值，但在未独立备份和删除授权前仍保留。现有物理实例复用为 V2 预生产：RDS 新建独立数据库和分角色账号，Tair 新建分角色账号并强制新 Key 前缀；正式生产另建物理实例。
- 边界：不删除或覆盖历史数据库/Key；不复用旧账号、旧密码和旧连接串；Tair Key 前缀不是安全权限边界；本决定不授权执行云资源写入。
- 回滚：新对象无数据时才可在单独批准后删除；有 schema/数据后保留现场并按备份或前向修复处理。
- 批准人：用户

### INFRA-022：第一版 worker 采用 HTTP-only 数据访问边界

- 决策编号：`INFRA-022`
- 日期：2026-08-01
- 状态：`ACCEPTED`
- 背景：当前 `dispatch-event-worker.mjs` 只通过 `INTERNAL_CRON_SECRET` 调用 app 的受保护内部端点，数据库事务、outbox 处理和 Tair 访问均由 app 执行。若仍向 worker 注入 RDS/Tair 凭据，会形成未使用的长期秘密和虚假的权限隔离。
- 决定：第一版 worker 不注入 `DATABASE_URL`、`REDIS_URL`、owner 凭据或高德服务端 Key；只持有内部调用秘密、目标 origin 和日志配置。已创建的 RDS/Tair worker 账号保留为未授权预留账号，不删除、不注入。
- 影响：app 获得业务运行所需最小权限；worker 的网络和秘密面更小。未来若改为 worker 直接处理数据，必须重新设计事务、并发、权限、秘密轮换和回退，不得直接启用预留账号。
- 回滚或退出条件：新的权威实现明确要求 worker 直接访问数据层，并通过独立代码、权限和运行验收后，替代本决定。
- 批准人：用户

### INFRA-023：暂以公网 IP 承载预生产演示

- 决策编号：`INFRA-023`
- 日期：2026-08-08
- 状态：`ACCEPTED`
- 背景：当前优先目标是在最低成本下完成产品功能和设计展示，暂不购买域名、不办理备案，也不把预生产入口宣布为正式生产。
- 决定：当前 Gate 3/Gate 4 阶段允许通过现有 ECS 公网 IP 和 Nginx 自签名 HTTPS 向受控演示对象提供预生产展示；其他电脑和不同网络可以访问，但必须保留证书警告和环境身份说明。
- 不选择什么：当前不购买正式域名、不申请正式域名证书、不办理正式上线所需备案/许可，也不通过降级纯 HTTP 或跳过证书校验冒充可信 HTTPS。
- 影响：域名、备案和浏览器受信任证书不阻断预生产功能验收；演示环境只使用演示或脱敏数据，关闭公开注册和演示凭据，公网只开放 Nginx 必要端口。Gate 3、第二轮并行和 Gate 4 的原闸门顺序不变。
- 回滚或退出条件：正式生产切换前必须完成正式域名、适用备案/许可和受信任证书验收；云平台或适用规则不允许当前展示方式时立即停止对外展示，不得绕过限制。
- 关联文档：`deployment-guide-v2.md` §8/§13、`infrastructure-v2.md` §7/§8、`docs/status/2026-08-08-gate3r-public-ip-demo-ingress.md`
- 批准人：用户

## 3. R7 裁决完成表

| 原编号 | 事项 | R7 状态 | 冻结结论 |
|---|---|---|---|
| `OPEN-001` | 阿里云生产地域 | `ACCEPTED` | 跟随正式 RDS；具体地域只读核验后登记（`INFRA-013`） |
| `OPEN-002` | 第一版可用性 | `ACCEPTED` | 单 ECS 试运行，接受单点与维护窗口（`INFRA-014`） |
| `OPEN-003` | RPO | `ACCEPTED` | 设计目标不超过 5 分钟（`INFRA-015`） |
| `OPEN-004` | RTO | `ACCEPTED` | 设计目标不超过 2 小时（`INFRA-015`） |
| `OPEN-005` | 数据与日志保留 | `ACCEPTED` | 位置 90 天、业务审计 1 年、运行 30 天、安全 180 天（`INFRA-016`） |
| `OPEN-006` | CloudBase 是否保留 | `ACCEPTED` | 不作为持续环境；不授权删除现有资源（`INFRA-017`） |
| `OPEN-007` | OSS 是否启用 | `NOT APPLICABLE` | 当前无文件业务，第一版不启用（`INFRA-019`） |
| `OPEN-008` | 集中日志产品 | `ACCEPTED` | 第一版使用 SLS（`INFRA-018`） |
| `OPEN-009` | 实施型事项 | `DEFERRED` | 公网 IP 预生产演示已由 `INFRA-023` 单独批准；正式域名/备案/可信证书和其余正式生产事项仍延后实施（`INFRA-020`、`INFRA-023`） |

## 4. 被替代历史说明

Railway 曾在 Gate 3 返修期间被裁决为正式 app/worker 平台。该结论由 `INFRA-001` 与 `INFRA-002` 替代。历史部署、验证结果和 ID 应继续存在于审计记录中，但不得继续定义生产架构、发布流程或运维基线。

## 5. 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| V2.0-r1 | 2026-07-30 | 冻结 INFRA-001～020 与 R7 基础设施裁决 |
| V2.0-r2 | 2026-08-01 | 增加 INFRA-021 预生产资源复用边界与 INFRA-022 HTTP-only worker 权限边界 |
| V2.0-r3 | 2026-08-08 | 增加 INFRA-023：公网 IP 预生产演示获准，正式域名、备案与受信任证书延后且不改变 Gate 顺序 |
